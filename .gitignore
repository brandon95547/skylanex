node_modules/
.DS_Store
*.log

# dist/ is committed on purpose (prod serves it), but the video copies are not —
# ~76MB duplicated on every clone. prod-deploy.sh recreates dist/videos from
# assets/videos, which IS tracked.
dist/videos/
